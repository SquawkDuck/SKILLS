interface:
  display_name: "Hit Me Plan"
  short_description: "Build feature-root plan.MD, then Plan mode"
  default_prompt: "Use $hit-me-plan when I say hit me once to build a feature-root plan.MD through one question at a time, finalize it when I say there is enough information, then hand off by entering Plan mode."
